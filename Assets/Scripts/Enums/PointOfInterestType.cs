namespace VARLab.CCSIF
{
    /// <summary>
    /// An enum for the differnt point of interests that a POI can belong to.
    /// </summary>
    public enum PointOfInterestType 
    {
        FrontCab = 0,
        DriverSideCab = 1,
        PassengerSideCab = 2,
        DriverSideMiddleTrailer = 3,
        PassengerSideMiddleTrailer = 4,
        DriverSideBackTrailer = 5,
        PassengerSideBackTrailer = 6,
        RearTrailer = 7,
    }
}
