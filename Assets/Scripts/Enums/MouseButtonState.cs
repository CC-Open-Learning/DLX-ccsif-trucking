namespace VARLab.CCSIF
{
    /// <summary>
    /// An enum used to track the current state of the mouse pointer.
    /// </summary>
    public enum MouseButtonState
    {
        Neutral = 0,
        Down = 1,
        Up = 2
    }
}
